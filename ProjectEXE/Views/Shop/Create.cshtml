@model ProjectEXE.ViewModel.ShopViewModel.CreateShopViewModel
@{
    ViewData["Title"] = "Tạo gian hàng mới";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0"><i class="fas fa-store me-2"></i>Tạo gian hàng mới</h3>
                </div>
                <div class="card-body">
                    <p class="text-muted mb-4">
                        Để bắt đầu bán hàng trên REVA, bạn cần tạo một gian hàng của riêng mình.
                        Vui lòng điền đầy đủ thông tin dưới đây để thiết lập gian hàng của bạn.
                    </p>

                    <form asp-action="Create" method="post" enctype="multipart/form-data">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                        <div class="mb-4">
                            <label asp-for="ShopName" class="form-label fw-bold"></label>
                            <input asp-for="ShopName" class="form-control" placeholder="Nhập tên gian hàng của bạn" />
                            <span asp-validation-for="ShopName" class="text-danger"></span>
                            <div class="form-text">Tên gian hàng sẽ hiển thị cho người mua và không thể trùng với gian hàng khác</div>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Description" class="form-label fw-bold"></label>
                            <textarea asp-for="Description" class="form-control" rows="4" placeholder="Mô tả ngắn gọn về gian hàng của bạn"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="ProfileImage" class="form-label fw-bold"></label>
                            <input asp-for="ProfileImage" class="form-control" type="file" accept="image/*" />
                            <span asp-validation-for="ProfileImage" class="text-danger"></span>
                            <div class="form-text">Tải lên ảnh đại diện cho gian hàng của bạn (định dạng JPG, PNG)</div>
                        </div>

                        <div class="mb-4">
                            <label asp-for="ContactInfo" class="form-label fw-bold"></label>
                            <textarea asp-for="ContactInfo" class="form-control" rows="3" placeholder="Nhập thông tin liên hệ (số điện thoại, email, địa chỉ...)"></textarea>
                            <span asp-validation-for="ContactInfo" class="text-danger"></span>
                            <div class="form-text">Thông tin liên hệ giúp khách hàng dễ dàng liên lạc với bạn</div>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="fas fa-store-alt me-2"></i>Tạo gian hàng
                            </button>
                            <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-2"></i>Quay lại
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}